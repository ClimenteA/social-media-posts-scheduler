from core.logger import log


# TODO
# process video to be postable on tiktok, facebook, instagram 
# make it 60 seconds, make sure resolution is ok.

def make_video_postable(video_path: str, text: str):
    try:
        pass
    except Exception as err:
        log.exception(err)
